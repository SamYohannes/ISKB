

def hotel_choice(number):
    if number 1:
        return 100
    if number 2:
        return 150
    if number 3: 200


def staying_how_long(days):
    if days < 3:
        return 40 * days
    elif days >= 3 and days < 7:
        return days * 40 - 20
    elif days >= 7:
        return days * 40 - 50

    def trip_cost(city,days):
        totalcost= hotel_choice(number)+staying_how_long(days)
        return totalcost
